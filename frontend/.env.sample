# API Configuration
VITE_API_BASE=/api

# Sentry Error Tracking (Optional)
# Only set this if you want to enable Sentry error tracking
# Get your DSN from https://sentry.io/settings/projects/
# VITE_SENTRY_DSN=https://your-dsn@sentry.io/your-project-id

# Release Version (Optional, for Sentry)
# Automatically set in CI/CD, but can be overridden locally
# VITE_RELEASE=local-dev
